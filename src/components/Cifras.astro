---
/** Cifras.astro — Banda oscura con contadores animados.
 *  Sobre fondo oscuro los glows teal lucen de verdad (contraste = wow).
 *  Cifras ancladas al copy real (hero, Pilares, FacturasRecibidas). No inventadas.
 *  Los números cuentan al entrar en viewport (JS en Base.astro, gated reduced-motion).
 */
const cifras = [
  { n: 200, suffix: "", label: "horas al año en papeleo y trámites, de media por autónomo" },
  { n: 90, suffix: " %", label: "de los autónomos dice que la burocracia no para de crecer" },
  { n: 5, suffix: " s", label: "lo que tardas en aprobar una factura con KonquerAI" },
];
---
<section class="cifras" id="cifras" aria-label="KonquerAI en números">
  <span class="cifras-glow" aria-hidden="true"></span>
  <div class="grain" aria-hidden="true"></div>
  <div class="container-narrow cifras-grid reveal-stagger">
    {cifras.map((c) => (
      <div class="cifra reveal">
        <span class="cifra-num" data-counter={c.n} data-suffix={c.suffix}>0{c.suffix}</span>
        <p class="cifra-label">{c.label}</p>
      </div>
    ))}
  </div>
  <p class="cifras-fuente reveal">
    Fuente: <a href="https://www.autonomosyemprendedor.es/articulo/info-ata/autonomos-han-invertido-200-horas-3000-euros-tramites-burocraticos-2025-ata/20251219150531047332.html" target="_blank" rel="noopener">ATA, 2025</a> (papeleo y burocracia). El dato de 5&nbsp;s es el tiempo de aprobación de una factura en la app.
  </p>
</section>

<style>
  .cifras {
    position: relative;
    overflow: hidden;
    background: var(--color-ink);
    padding-block: 4.5rem;
  }

  .cifras-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 60rem;
    height: 40rem;
    max-width: 120vw;
    transform: translate(-50%, -50%);
    background:
      radial-gradient(40% 50% at 30% 42%, rgba(0, 209, 178, 0.22), transparent 70%),
      radial-gradient(36% 46% at 72% 58%, rgba(255, 138, 0, 0.12), transparent 70%);
    filter: blur(30px);
    pointer-events: none;
  }

  .cifras :global(.grain) {
    opacity: 0.06;
  }

  .cifras-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
    text-align: center;
  }

  @media (min-width: 768px) {
    .cifras-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
    }
  }

  .cifra-num {
    display: block;
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: clamp(3rem, 7vw, 4.5rem);
    line-height: 1;
    color: var(--color-teal);
    letter-spacing: -0.02em;
    text-shadow: 0 0 28px rgba(0, 209, 178, 0.45);
    font-variant-numeric: tabular-nums;
  }

  .cifra-label {
    margin-top: 0.75rem;
    color: rgba(255, 255, 255, 0.82);
    font-size: 0.9375rem;
    line-height: 1.5;
    max-width: 22rem;
    margin-inline: auto;
  }

  .cifras-fuente {
    position: relative;
    z-index: 1;
    max-width: 44rem;
    margin: 2.5rem auto 0;
    padding-inline: 1.5rem;
    text-align: center;
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.8125rem;
    line-height: 1.5;
  }
  .cifras-fuente a {
    color: rgba(255, 255, 255, 0.72);
    text-decoration: underline;
  }
</style>
